import ApiDoc from '@site/src/components/ApiDoc';

# ALayoutInflater

Referência completa da API de `ALayoutInflater`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class ALayoutInflater
```

<ApiDoc
  methods={[
    {
      signature: "inflate(File)",
      returns: "View",
      description: <>Executa <code>inflate</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  staticMethods={[
    {
      signature: "inflate(String)",
      returns: "View",
      description: <>Executa a operação inflate usando os parâmetros informados (inProjectPath).</>
    },
    {
      signature: "inflate(java.io.File)",
      returns: "Object",
      description: <>Retorna o resultado de <code>inflate</code>.</>
    },
    {
      signature: "inflate(Resources.File)",
      returns: "View",
      description: <>Executa a operação inflate usando os parâmetros informados (file).</>
    },
    {
      signature: "inflateXML(String)",
      returns: "View",
      description: <>Executa a operação inflateXML usando os parâmetros informados (xml).</>
    }
  ]}
/>
