import ApiDoc from '@site/src/components/ApiDoc';

# UIAspectRatio

Referência completa da API de `UIAspectRatio`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIAspectRatio extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIAspectRatio()",
      description: <>Cria uma nova inst?ncia de UIAspectRatio.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "mode",
      type: "Mode",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMode</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "proportion",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getProportion</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
