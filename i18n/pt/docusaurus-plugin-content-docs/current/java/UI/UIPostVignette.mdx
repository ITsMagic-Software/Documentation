import ApiDoc from '@site/src/components/ApiDoc';

# UIPostVignette

Referência completa da API de `UIPostVignette`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class UIPostVignette extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIPostVignette()",
      description: <>Cria uma nova inst?ncia de UIPostVignette.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getColor</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "feather",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getFeather</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "midPoint",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMidPoint</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "roundness",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRoundness</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getColor()",
      returns: "Color",
      description: <>Retorna o valor atual de <code>color</code>.</>,
    },
    {
      signature: "getFeather()",
      returns: "float",
      description: <>Retorna o valor atual de <code>feather</code>.</>,
    },
    {
      signature: "getMidPoint()",
      returns: "float",
      description: <>Retorna o valor atual de <code>midPoint</code>.</>,
    },
    {
      signature: "getRoundness()",
      returns: "float",
      description: <>Retorna o valor atual de <code>roundness</code>.</>,
    },
    {
      signature: "setColor(Color)",
      returns: "void",
      description: <>Define <code>color</code>.</>,
    },
    {
      signature: "setFeather(float)",
      returns: "void",
      description: <>Define <code>feather</code>.</>,
    },
    {
      signature: "setMidPoint(float)",
      returns: "void",
      description: <>Define <code>midPoint</code>.</>,
    },
    {
      signature: "setRoundness(float)",
      returns: "void",
      description: <>Define <code>roundness</code>.</>,
    }
  ]}
/>
