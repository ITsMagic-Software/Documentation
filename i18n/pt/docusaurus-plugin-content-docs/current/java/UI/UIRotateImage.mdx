import ApiDoc from '@site/src/components/ApiDoc';

# UIRotateImage

Referência completa da API de `UIRotateImage`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIRotateImage extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIRotateImage()",
      description: <>Cria uma nova inst?ncia de UIRotateImage.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "angle",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAngle</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "texture",
      type: "Texture",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTexture</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getAngle()",
      returns: "float",
      description: <>Retorna o valor atual de <code>angle</code>.</>,
    },
    {
      signature: "setAngle(float)",
      returns: "void",
      description: <>Define <code>angle</code>.</>,
    },
    {
      signature: "getTexture()",
      returns: "Texture",
      description: <>Retorna o valor atual de <code>texture</code>.</>,
    },
    {
      signature: "setTexture(Texture)",
      returns: "void",
      description: <>Define <code>texture</code>.</>,
    }
  ]}
/>
