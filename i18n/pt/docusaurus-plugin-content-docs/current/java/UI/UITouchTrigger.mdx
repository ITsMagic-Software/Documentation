import ApiDoc from '@site/src/components/ApiDoc';

# UITouchTrigger

Referência completa da API de `UITouchTrigger`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UITouchTrigger extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UITouchTrigger()",
      description: <>Cria uma nova inst?ncia de UITouchTrigger.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "selectedTouch",
      type: "Touch",
      access: "get",
      description: <>Atributo virtual exposto por <code>getSelectedTouch</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getSelectedTouch()",
      returns: "Touch",
      description: <>Retorna o valor atual de <code>selectedTouch</code>.</>,
    },
    {
      signature: "isDown()",
      returns: "boolean",
      description: <>Retorna se <code>down</code> est? ativado.</>,
    },
    {
      signature: "isPressed()",
      returns: "boolean",
      description: <>Retorna se <code>pressed</code> est? ativado.</>,
    },
    {
      signature: "isUp()",
      returns: "boolean",
      description: <>Retorna se <code>up</code> est? ativado.</>,
    }
  ]}
/>
