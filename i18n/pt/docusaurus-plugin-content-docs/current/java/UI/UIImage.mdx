import ApiDoc from '@site/src/components/ApiDoc';

# UIImage

Referência completa da API de `UIImage`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIImage extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIImage()",
      description: <>Cria uma nova inst?ncia de UIImage.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "border",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getBorder</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getColor</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "texture",
      type: "Texture",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTexture</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
