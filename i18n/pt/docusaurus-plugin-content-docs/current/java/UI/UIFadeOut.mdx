import ApiDoc from '@site/src/components/ApiDoc';

# UIFadeOut

Referência completa da API de `UIFadeOut`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class UIFadeOut extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIFadeOut()",
      description: <>Cria uma nova inst?ncia de UIFadeOut.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "opacity",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getOpacity</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getOpacity()",
      returns: "float",
      description: <>Retorna o valor atual de <code>opacity</code>.</>,
    },
    {
      signature: "setOpacity(float)",
      returns: "void",
      description: <>Define <code>opacity</code>.</>,
    }
  ]}
/>
