import ApiDoc from '@site/src/components/ApiDoc';

# UIDynamicJoystick

Referência completa da API de `UIDynamicJoystick`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIDynamicJoystick extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIDynamicJoystick()",
      description: <>Cria uma nova inst?ncia de UIDynamicJoystick.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "backgroundColor",
      type: "Color",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getBackgroundColor</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "backgroundTexture",
      type: "Texture",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getBackgroundTexture</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "handleSize",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getHandleSize</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "handlerColor",
      type: "Color",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getHandlerColor</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "handlerTexture",
      type: "Texture",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getHandlerTexture</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "size",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSize</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getValue()",
      returns: "Vector2",
      description: <>Retorna o valor atual de <code>value</code>.</>,
    },
    {
      signature: "getBackgroundColor()",
      returns: "Color",
      description: <>Retorna o valor atual de <code>backgroundColor</code>.</>,
    },
    {
      signature: "getBackgroundTexture()",
      returns: "Texture",
      description: <>Retorna o valor atual de <code>backgroundTexture</code>.</>,
    },
    {
      signature: "getHandleSize()",
      returns: "int",
      description: <>Retorna o valor atual de <code>handleSize</code>.</>,
    },
    {
      signature: "getHandlerColor()",
      returns: "Color",
      description: <>Retorna o valor atual de <code>handlerColor</code>.</>,
    },
    {
      signature: "getHandlerTexture()",
      returns: "Texture",
      description: <>Retorna o valor atual de <code>handlerTexture</code>.</>,
    },
    {
      signature: "getSize()",
      returns: "int",
      description: <>Retorna o valor atual de <code>size</code>.</>,
    },
    {
      signature: "setBackgroundColor(Color)",
      returns: "void",
      description: <>Define <code>backgroundColor</code>.</>,
    },
    {
      signature: "setBackgroundTexture(Texture)",
      returns: "void",
      description: <>Define <code>backgroundTexture</code>.</>,
    },
    {
      signature: "setHandleSize(int)",
      returns: "void",
      description: <>Define <code>handleSize</code>.</>,
    },
    {
      signature: "setHandlerColor(Color)",
      returns: "void",
      description: <>Define <code>handlerColor</code>.</>,
    },
    {
      signature: "setHandlerTexture(Texture)",
      returns: "void",
      description: <>Define <code>handlerTexture</code>.</>,
    },
    {
      signature: "setSize(int)",
      returns: "void",
      description: <>Define <code>size</code>.</>,
    }
  ]}
/>
