import ApiDoc from '@site/src/components/ApiDoc';

# UIFlexLayout

Referência completa da API de `UIFlexLayout`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIFlexLayout extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIFlexLayout()",
      description: <>Cria uma nova inst?ncia de UIFlexLayout.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "spacing",
      type: "int",
      access: "set",
      description: <>Atributo virtual exposto por <code>setSpacing</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getSpacing()",
      returns: "int",
      description: <>Retorna o valor atual de <code>spacing</code>.</>,
    },
    {
      signature: "setSpacing(int)",
      returns: "void",
      description: <>Define <code>spacing</code>.</>,
    }
  ]}
/>
