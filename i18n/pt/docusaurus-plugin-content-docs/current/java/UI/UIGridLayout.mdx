import ApiDoc from '@site/src/components/ApiDoc';

# UIGridLayout

Referência completa da API de `UIGridLayout`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIGridLayout extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIGridLayout()",
      description: <>Cria uma nova inst?ncia de UIGridLayout.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "autoColumns",
      type: "boolean",
      access: "set",
      description: <>Atributo virtual exposto por <code>setAutoColumns</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "columns",
      type: "int",
      access: "set",
      description: <>Atributo virtual exposto por <code>setColumns</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "spacing",
      type: "int",
      access: "set",
      description: <>Atributo virtual exposto por <code>setSpacing</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getSpacing()",
      returns: "int",
      description: <>Executa <code>getSpacing</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getColumns()",
      returns: "int",
      description: <>Executa <code>getColumns</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "isAutoColumns()",
      returns: "boolean",
      description: <>Executa <code>isAutoColumns</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
