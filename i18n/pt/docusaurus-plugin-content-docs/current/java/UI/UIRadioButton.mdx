import ApiDoc from '@site/src/components/ApiDoc';

# UIRadioButton

Referencia completa da API de `UIRadioButton`, incluindo construtores, atributos, atributos virtuais e metodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIRadioButton extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIRadioButton()",
      description: <>Cria uma nova inst?ncia de UIRadioButton.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "checked",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>isChecked</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "pressed",
      type: "boolean",
      access: "get",
      description: <>Atributo virtual exposto por <code>isPressed</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "down",
      type: "boolean",
      access: "get",
      description: <>Atributo virtual exposto por <code>isDown</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "up",
      type: "boolean",
      access: "get",
      description: <>Atributo virtual exposto por <code>isUp</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getColor</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "checkedTexture",
      type: "Texture",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getCheckedTexture</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "uncheckedTexture",
      type: "Texture",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getUncheckedTexture</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "isChecked()",
      returns: "boolean",
      description: <>Retorna se <code>checked</code> est? ativado.</>,
    },
    {
      signature: "setChecked(boolean)",
      returns: "void",
      description: <>Define <code>checked</code>.</>,
    },
    {
      signature: "isPressed()",
      returns: "boolean",
      description: <>Retorna se <code>pressed</code> est? ativado.</>,
    },
    {
      signature: "isDown()",
      returns: "boolean",
      description: <>Retorna se <code>down</code> est? ativado.</>,
    },
    {
      signature: "isUp()",
      returns: "boolean",
      description: <>Retorna se <code>up</code> est? ativado.</>,
    },
    {
      signature: "getColor()",
      returns: "Color",
      description: <>Retorna o valor atual de <code>color</code>.</>,
    },
    {
      signature: "getCheckedTexture()",
      returns: "Texture",
      description: <>Retorna o valor atual de <code>checkedTexture</code>.</>,
    },
    {
      signature: "getUncheckedTexture()",
      returns: "Texture",
      description: <>Retorna o valor atual de <code>uncheckedTexture</code>.</>,
    },
    {
      signature: "setColor(Color)",
      returns: "void",
      description: <>Define <code>color</code>.</>,
    },
    {
      signature: "setCheckedTexture(Texture)",
      returns: "void",
      description: <>Define <code>checkedTexture</code>.</>,
    },
    {
      signature: "setUncheckedTexture(Texture)",
      returns: "void",
      description: <>Define <code>uncheckedTexture</code>.</>,
    }
  ]}
/>
