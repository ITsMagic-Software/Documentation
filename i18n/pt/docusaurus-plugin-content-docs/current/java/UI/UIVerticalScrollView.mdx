import ApiDoc from '@site/src/components/ApiDoc';

# UIVerticalScrollView

Referência completa da API de `UIVerticalScrollView`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIVerticalScrollView extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIVerticalScrollView()",
      description: <>Cria uma nova inst?ncia de UIVerticalScrollView.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "scroll",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getScroll</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getScroll()",
      returns: "int",
      description: <>Retorna o valor atual de <code>scroll</code>.</>,
    },
    {
      signature: "setScroll(int)",
      returns: "void",
      description: <>Define <code>scroll</code>.</>,
    },
    {
      signature: "getSensitivity()",
      returns: "float",
      description: <>Retorna o valor atual de <code>sensitivity</code>.</>,
    },
    {
      signature: "setSensitivity(float)",
      returns: "void",
      description: <>Define <code>sensitivity</code>.</>,
    }
  ]}
/>
