import ApiDoc from '@site/src/components/ApiDoc';

# UISlideArea

Referência completa da API de `UISlideArea`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UISlideArea extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UISlideArea()",
      description: <>Cria uma nova inst?ncia de UISlideArea.</>
    }
  ]}
  attributes={[]}
  methods={[
    {
      signature: "getValue()",
      returns: "Vector2",
      description: <>Retorna o valor atual de <code>value</code>.</>,
    },
    {
      signature: "getDeadzone()",
      returns: "float",
      description: <>Retorna o valor atual de <code>deadzone</code>.</>,
    },
    {
      signature: "setDeadzone(float)",
      returns: "void",
      description: <>Define <code>deadzone</code>.</>,
    },
    {
      signature: "getIntensityMultiplier()",
      returns: "float",
      description: <>Retorna o valor atual de <code>intensityMultiplier</code>.</>,
    },
    {
      signature: "setIntensityMultiplier(float)",
      returns: "void",
      description: <>Define <code>intensityMultiplier</code>.</>,
    }
  ]}
/>
