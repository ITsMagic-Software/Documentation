import ApiDoc from '@site/src/components/ApiDoc';

# UISlideArea

Referência completa da API de `UISlideArea`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UISlideArea extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UISlideArea()",
      description: <>Cria uma nova inst?ncia de UISlideArea.</>
    }
  ]}
  attributes={[]}
  methods={[
    {
      signature: "getValue()",
      returns: "Vector2",
      description: <>Retorna a propriedade <code>propriedade </code>valor value atual``.</>
    }
  ]}
/>
