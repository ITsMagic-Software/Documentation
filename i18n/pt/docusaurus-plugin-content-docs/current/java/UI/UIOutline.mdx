import ApiDoc from '@site/src/components/ApiDoc';

# UIOutline

Referência completa da API de `UIOutline`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class UIOutline extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIOutline()",
      description: <>Cria uma nova inst?ncia de UIOutline.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "outlineColor",
      type: "Color",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getOutlineColor</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "thickness",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getThickness</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "threshold",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getThreshold</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getOutlineColor()",
      returns: "Color",
      description: <>Retorna o valor atual de <code>outlineColor</code>.</>,
    },
    {
      signature: "getThickness()",
      returns: "float",
      description: <>Retorna o valor atual de <code>thickness</code>.</>,
    },
    {
      signature: "getThreshold()",
      returns: "float",
      description: <>Retorna o valor atual de <code>threshold</code>.</>,
    },
    {
      signature: "setOutlineColor(Color)",
      returns: "void",
      description: <>Define <code>outlineColor</code>.</>,
    },
    {
      signature: "setThickness(float)",
      returns: "void",
      description: <>Define <code>thickness</code>.</>,
    },
    {
      signature: "setThreshold(float)",
      returns: "void",
      description: <>Define <code>threshold</code>.</>,
    }
  ]}
/>
