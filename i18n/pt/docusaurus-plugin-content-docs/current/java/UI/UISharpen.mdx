import ApiDoc from '@site/src/components/ApiDoc';

# UISharpen

Referência completa da API de `UISharpen`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class UISharpen extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UISharpen()",
      description: <>Cria uma nova inst?ncia de UISharpen.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "edgeThreshold",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getEdgeThreshold</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "intensity",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getIntensity</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "sharpenStrength",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSharpenStrength</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getIntensity()",
      returns: "float",
      description: <>Retorna o valor atual de <code>intensity</code>.</>,
    },
    {
      signature: "getSharpenStrength()",
      returns: "float",
      description: <>Retorna o valor atual de <code>sharpenStrength</code>.</>,
    },
    {
      signature: "getEdgeThreshold()",
      returns: "float",
      description: <>Retorna o valor atual de <code>edgeThreshold</code>.</>,
    },
    {
      signature: "setIntensity(float)",
      returns: "void",
      description: <>Define <code>intensity</code>.</>,
    },
    {
      signature: "setSharpenStrength(float)",
      returns: "void",
      description: <>Define <code>sharpenStrength</code>.</>,
    },
    {
      signature: "setEdgeThreshold(float)",
      returns: "void",
      description: <>Define <code>edgeThreshold</code>.</>,
    }
  ]}
/>
