import ApiDoc from '@site/src/components/ApiDoc';

# UIChromaticAberration

Referência completa da API de `UIChromaticAberration`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class UIChromaticAberration extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIChromaticAberration()",
      description: <>Cria uma nova inst?ncia de UIChromaticAberration.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "falloff",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getFalloff</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "strength",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getStrength</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
