import ApiDoc from '@site/src/components/ApiDoc';

# UIWeight

Referência completa da API de `UIWeight`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIWeight extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIWeight()",
      description: <>Cria uma nova inst?ncia de UIWeight.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "weight",
      type: "float",
      access: "set",
      description: <>Atributo virtual exposto por <code>setWeight</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getWeight()",
      returns: "float",
      description: <>Executa <code>getWeight</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
