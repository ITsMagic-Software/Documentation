import ApiDoc from '@site/src/components/ApiDoc';

# UIBackgroundBlur

Referência completa da API de `UIBackgroundBlur`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class UIBackgroundBlur extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIBackgroundBlur()",
      description: <>Cria uma nova inst?ncia de UIBackgroundBlur.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "blurRadius",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getBlurRadius</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "quality",
      type: "Quality",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getQuality</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getBlurRadius()",
      returns: "float",
      description: <>Retorna o valor atual de <code>blurRadius</code>.</>,
    },
    {
      signature: "getQuality()",
      returns: "Quality",
      description: <>Retorna o valor atual de <code>quality</code>.</>,
    },
    {
      signature: "setBlurRadius(float)",
      returns: "void",
      description: <>Define <code>blurRadius</code>.</>,
    },
    {
      signature: "setQuality(Quality)",
      returns: "void",
      description: <>Define <code>quality</code>.</>,
    }
  ]}
/>
