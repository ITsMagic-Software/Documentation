import ApiDoc from '@site/src/components/ApiDoc';

# UI3DCameraAimInputSystem

Referência completa da API de `UI3DCameraAimInputSystem`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UI3DCameraAimInputSystem extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UI3DCameraAimInputSystem()",
      description: <>Cria uma nova inst?ncia de UI3DCameraAimInputSystem.</>
    }
  ]}
  attributes={[]}
  methods={[
    {
      signature: "performClick()",
      returns: "void",
      description: <>Executa <code>performClick</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
