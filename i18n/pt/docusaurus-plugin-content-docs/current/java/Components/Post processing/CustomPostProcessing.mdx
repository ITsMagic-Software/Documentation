import ApiDoc from '@site/src/components/ApiDoc';

# CustomPostProcessing

Referência completa da API de `CustomPostProcessing`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class CustomPostProcessing extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "CustomPostProcessing(CustomPostProcessing)",
      description: <>Executa <code>CustomPostProcessing</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "CustomPostProcessing(Engine)",
      description: <>Executa <code>CustomPostProcessing</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "CustomPostProcessing()",
      description: <>Executa <code>CustomPostProcessing</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "CustomPostProcessing",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "shaderFile",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getShaderFile</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "time",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTime</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "setParam(String, float)",
      returns: "void",
      description: <>Executa <code>setParam</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "setParam(String, Color)",
      returns: "void",
      description: <>Executa <code>setParam</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "setParam(String, Texture)",
      returns: "void",
      description: <>Executa <code>setParam</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "setParam(String, Object)",
      returns: "void",
      description: <>Executa <code>setParam</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "setParam(String, float[])",
      returns: "void",
      description: <>Executa <code>setParam</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
