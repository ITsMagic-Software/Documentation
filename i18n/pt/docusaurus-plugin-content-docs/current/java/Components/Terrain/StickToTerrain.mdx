import ApiDoc from '@site/src/components/ApiDoc';

# StickToTerrain

Refer?ncia completa da API de `StickToTerrain`, incluindo construtores, atributos, atributos virtuais e m?todos.

**Pacote:** `JAVARuntime`

**Categoria:** Terrain

**Componente:** sim

---

```java
public class StickToTerrain extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "StickToTerrain(StickToTerrain)",
      description: <>Cria uma nova inst?ncia de StickToTerrain.</>,
    },
    {
      signature: "StickToTerrain(Engine)",
      description: <>Cria uma nova inst?ncia de StickToTerrain.</>,
    },
    {
      signature: "StickToTerrain()",
      description: <>Cria uma nova inst?ncia de StickToTerrain.</>,
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "StickToTerrain",
      description: <>Representa o valor de <code>instance</code> armazenado nesta inst?ncia. Leia ou atribua para atualizar a propriedade.</>,
    }
  ]}
/>
