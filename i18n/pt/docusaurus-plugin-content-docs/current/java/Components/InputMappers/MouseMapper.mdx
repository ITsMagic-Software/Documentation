import ApiDoc from '@site/src/components/ApiDoc';

# MouseMapper

Referência completa da API de `MouseMapper`, incluindo construtores, atributos e atributos virtuais.

**Pacote:** `JAVARuntime`

**Categoria:** Input Mappers

**Componente:** sim

---

```java
public class MouseMapper extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "MouseMapper()",
      description: <>Cria uma nova instância de MouseMapper.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "MouseMapper",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "mouseButton",
      type: "Button",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMouseButton</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "outputKey",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getOutputKey</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
