import ApiDoc from '@site/src/components/ApiDoc';

# KeyboardMapper

Referência completa da API de `KeyboardMapper`, incluindo construtores, atributos e atributos virtuais.

**Pacote:** `JAVARuntime`

**Categoria:** Input Mappers

**Componente:** sim

---

```java
public class KeyboardMapper extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "KeyboardMapper()",
      description: <>Cria uma nova instância de KeyboardMapper.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "KeyboardMapper",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "keyboardKey",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getKeyboardKey</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "outputKey",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getOutputKey</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
