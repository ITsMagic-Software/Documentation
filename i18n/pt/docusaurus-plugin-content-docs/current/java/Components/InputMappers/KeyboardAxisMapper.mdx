import ApiDoc from '@site/src/components/ApiDoc';

# KeyboardAxisMapper

Referência completa da API de `KeyboardAxisMapper`, incluindo construtores, atributos e atributos virtuais.

**Pacote:** `JAVARuntime`

**Categoria:** Input Mappers

**Componente:** sim

---

```java
public class KeyboardAxisMapper extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "KeyboardAxisMapper()",
      description: <>Cria uma nova instância de KeyboardAxisMapper.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "KeyboardAxisMapper",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "negativeKeyboardKey",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getNegativeKeyboardKey</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "positiveKeyboardKey",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getPositiveKeyboardKey</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "axisType",
      type: "AxisType",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAxisType</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "outputAxis",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getOutputAxis</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
