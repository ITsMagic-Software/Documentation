import ApiDoc from '@site/src/components/ApiDoc';

# MouseAxisMapper

Referência completa da API de `MouseAxisMapper`, incluindo construtores, atributos e atributos virtuais.

**Pacote:** `JAVARuntime`

**Categoria:** Input Mappers

**Componente:** sim

---

```java
public class MouseAxisMapper extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "MouseAxisMapper()",
      description: <>Cria uma nova instância de MouseAxisMapper.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "MouseAxisMapper",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "outputAxis",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getOutputAxis</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
