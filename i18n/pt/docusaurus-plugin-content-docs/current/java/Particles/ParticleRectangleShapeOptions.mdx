import ApiDoc from '@site/src/components/ApiDoc';

# ParticleRectangleShapeOptions

Refer?ncia completa da API de `ParticleRectangleShapeOptions`, incluindo construtores, atributos, atributos virtuais e m?todos.

**Pacote:** `JAVARuntime`

**Categoria:** Particles

**Componente:** n?o

---

```java
public class ParticleRectangleShapeOptions
```

<ApiDoc
  constructors={[
    {
      signature: "ParticleRectangleShapeOptions(ParticleRectangleShapeOptions)",
      description: <>Cria uma nova inst?ncia de ParticleRectangleShapeOptions.</>,
    },
    {
      signature: "ParticleRectangleShapeOptions(Engine)",
      description: <>Cria uma nova inst?ncia de ParticleRectangleShapeOptions.</>,
    },
    {
      signature: "ParticleRectangleShapeOptions()",
      description: <>Cria uma nova inst?ncia de ParticleRectangleShapeOptions.</>,
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "ParticleRectangleShapeOptions",
      description: <>Representa o valor de <code>instance</code> armazenado nesta inst?ncia. Leia ou atribua para atualizar a propriedade.</>,
    }
  ]}
  virtualAttributes={[
    {
      name: "height",
      type: "float",
      access: "get/set",
      description: <>Retorna o valor atual de <code>height</code>.</>,
    },
    {
      name: "length",
      type: "float",
      access: "get/set",
      description: <>Retorna o valor atual de <code>length</code>.</>,
    },
    {
      name: "randomizeRotation",
      type: "float",
      access: "get/set",
      description: <>Retorna o valor atual de <code>randomize rotation</code>.</>,
    },
    {
      name: "spherizeRotation",
      type: "float",
      access: "get/set",
      description: <>Retorna o valor atual de <code>spherize rotation</code>.</>,
    },
    {
      name: "width",
      type: "float",
      access: "get/set",
      description: <>Retorna o valor atual de <code>width</code>.</>,
    }
  ]}
/>
