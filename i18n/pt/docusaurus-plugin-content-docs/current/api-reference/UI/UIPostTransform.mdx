import ApiDoc from '@site/src/components/ApiDoc';

# UIPostTransform

Referência completa da API de `UIPostTransform`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class UIPostTransform extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIPostTransform()",
      description: <>Cria uma nova inst?ncia de UIPostTransform.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "fOV",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getFOV</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "pivot",
      type: "Vector2",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getPivot</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "position",
      type: "Vector2",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getPosition</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "rotation",
      type: "Vector3",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRotation</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "scale",
      type: "Vector2",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getScale</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getPosition()",
      returns: "Vector2",
      description: <>Retorna o valor atual de <code>position</code>.</>,
    },
    {
      signature: "getPivot()",
      returns: "Vector2",
      description: <>Retorna o valor atual de <code>pivot</code>.</>,
    },
    {
      signature: "getScale()",
      returns: "Vector2",
      description: <>Retorna o valor atual de <code>scale</code>.</>,
    },
    {
      signature: "getRotation()",
      returns: "Vector3",
      description: <>Retorna o valor atual de <code>rotation</code>.</>,
    },
    {
      signature: "setPosition(Vector2)",
      returns: "void",
      description: <>Define <code>position</code>.</>,
    },
    {
      signature: "setPivot(Vector2)",
      returns: "void",
      description: <>Define <code>pivot</code>.</>,
    },
    {
      signature: "setScale(Vector2)",
      returns: "void",
      description: <>Define <code>scale</code>.</>,
    },
    {
      signature: "setRotation(Vector3)",
      returns: "void",
      description: <>Define <code>rotation</code>.</>,
    },
    {
      signature: "getFOV()",
      returns: "float",
      description: <>Retorna o valor atual de <code>fOV</code>.</>,
    },
    {
      signature: "setFOV(float)",
      returns: "void",
      description: <>Define <code>fOV</code>.</>,
    }
  ]}
/>
