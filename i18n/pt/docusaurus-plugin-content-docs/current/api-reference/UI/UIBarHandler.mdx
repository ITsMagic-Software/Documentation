import ApiDoc from '@site/src/components/ApiDoc';

# UIBarHandler

Referência completa da API de `UIBarHandler`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** TODO

**Componente:** sim

---

```java
public class UIBarHandler extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIBarHandler()",
      description: <>Cria uma nova inst?ncia de UIBarHandler.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "align",
      type: "Align",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAlign</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getAlign()",
      returns: "Align",
      description: <>Retorna o valor atual de <code>align</code>.</>,
    },
    {
      signature: "setAlign(Align)",
      returns: "void",
      description: <>Define <code>align</code>.</>,
    }
  ]}
/>
