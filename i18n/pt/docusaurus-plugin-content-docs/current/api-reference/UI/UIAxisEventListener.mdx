import ApiDoc from '@site/src/components/ApiDoc';

# UIAxisEventListener

Referência completa da API de `UIAxisEventListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIAxisEventListener extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIAxisEventListener()",
      description: <>Cria uma nova inst?ncia de UIAxisEventListener.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "axisName",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAxisName</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "value",
      type: "Vector2",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getValue</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getAxisName()",
      returns: "String",
      description: <>Retorna o valor atual de <code>axisName</code>.</>,
    },
    {
      signature: "getValue()",
      returns: "Vector2",
      description: <>Retorna o valor atual de <code>value</code>.</>,
    },
    {
      signature: "setAxisName(String)",
      returns: "void",
      description: <>Define <code>axisName</code>.</>,
    },
    {
      signature: "setValue(float, float)",
      returns: "void",
      description: <>Define <code>value</code>.</>,
    },
    {
      signature: "setValue(Vector2)",
      returns: "void",
      description: <>Define <code>value</code>.</>,
    }
  ]}
/>
