import ApiDoc from '@site/src/components/ApiDoc';

# UITextView

Referência completa da API de `UITextView`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UITextView extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UITextView()",
      description: <>Cria uma nova inst?ncia de UITextView.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getColor</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "font",
      type: "Font",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getFont</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "text",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getText</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "textSize",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTextSize</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getColor()",
      returns: "Color",
      description: <>Retorna o valor atual de <code>color</code>.</>,
    },
    {
      signature: "setColor(Color)",
      returns: "void",
      description: <>Define <code>color</code>.</>,
    },
    {
      signature: "getOutlineColor()",
      returns: "Color",
      description: <>Retorna o valor atual de <code>outlineColor</code>.</>,
    },
    {
      signature: "setOutlineColor(Color)",
      returns: "void",
      description: <>Define <code>outlineColor</code>.</>,
    },
    {
      signature: "getText()",
      returns: "String",
      description: <>Retorna o valor atual de <code>text</code>.</>,
    },
    {
      signature: "setText(String)",
      returns: "void",
      description: <>Define <code>text</code>.</>,
    },
    {
      signature: "getTextSize()",
      returns: "float",
      description: <>Retorna o valor atual de <code>textSize</code>.</>,
    },
    {
      signature: "setTextSize(float)",
      returns: "void",
      description: <>Define <code>textSize</code>.</>,
    },
    {
      signature: "getOutlineDP()",
      returns: "float",
      description: <>Retorna o valor atual de <code>outlineDP</code>.</>,
    },
    {
      signature: "setOutlineDP(float)",
      returns: "void",
      description: <>Define <code>outlineDP</code>.</>,
    },
    {
      signature: "isAllCaps()",
      returns: "boolean",
      description: <>Retorna se <code>allCaps</code> est? ativado.</>,
    },
    {
      signature: "setAllCaps(boolean)",
      returns: "void",
      description: <>Define <code>allCaps</code>.</>,
    },
    {
      signature: "getFont()",
      returns: "Font",
      description: <>Retorna o valor atual de <code>font</code>.</>,
    },
    {
      signature: "setFont(Font)",
      returns: "void",
      description: <>Define <code>font</code>.</>,
    }
  ]}
/>
