import ApiDoc from '@site/src/components/ApiDoc';

# UIInputText

Referência completa da API de `UIInputText`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIInputText extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIInputText()",
      description: <>Cria uma nova inst?ncia de UIInputText.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getColor</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "editing",
      type: "boolean",
      access: "set",
      description: <>Atributo virtual exposto por <code>setEditing</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "text",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getText</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "textSize",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTextSize</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getColor()",
      returns: "Color",
      description: <>Retorna o valor atual de <code>color</code>.</>,
    },
    {
      signature: "getText()",
      returns: "String",
      description: <>Retorna o valor atual de <code>text</code>.</>,
    },
    {
      signature: "getTextSize()",
      returns: "float",
      description: <>Retorna o valor atual de <code>textSize</code>.</>,
    },
    {
      signature: "isEditing()",
      returns: "boolean",
      description: <>Retorna se <code>editing</code> est? ativado.</>,
    },
    {
      signature: "isExtractUI()",
      returns: "boolean",
      description: <>Retorna se <code>extractUI</code> est? ativado.</>,
    },
    {
      signature: "getExtractUITitle()",
      returns: "String",
      description: <>Retorna o valor atual de <code>extractUITitle</code>.</>,
    },
    {
      signature: "setColor(Color)",
      returns: "void",
      description: <>Define <code>color</code>.</>,
    },
    {
      signature: "setEditing(boolean)",
      returns: "void",
      description: <>Define <code>editing</code>.</>,
    },
    {
      signature: "setExtractUI(boolean)",
      returns: "void",
      description: <>Define <code>extractUI</code>.</>,
    },
    {
      signature: "setExtractUITitle(String)",
      returns: "void",
      description: <>Define <code>extractUITitle</code>.</>,
    },
    {
      signature: "setText(String)",
      returns: "void",
      description: <>Define <code>text</code>.</>,
    },
    {
      signature: "setTextSize(float)",
      returns: "void",
      description: <>Define <code>textSize</code>.</>,
    }
  ]}
/>
