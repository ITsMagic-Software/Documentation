import ApiDoc from '@site/src/components/ApiDoc';

# UILensDistortion

Referência completa da API de `UILensDistortion`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class UILensDistortion extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UILensDistortion()",
      description: <>Cria uma nova inst?ncia de UILensDistortion.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "border",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getBorder</code>. Acesse como um campo para ler ou escrever o valor interno.</>
    },
    {
      name: "center",
      type: "Vector2",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getCenter</code>. Acesse como um campo para ler ou escrever o valor interno.</>
    },
    {
      name: "intensity",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getIntensity</code>. Acesse como um campo para ler ou escrever o valor interno.</>
    },
    {
      name: "mode",
      type: "Mode",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMode</code>. Acesse como um campo para ler ou escrever o valor interno.</>
    }
  ]}
  methods={[
    {
      signature: "getBorder()",
      returns: "float",
      description: <>Retorna o valor atual de <code>border</code>.</>,
    },
    {
      signature: "getCenter()",
      returns: "Vector2",
      description: <>Retorna o valor atual de <code>center</code>.</>,
    },
    {
      signature: "getIntensity()",
      returns: "float",
      description: <>Retorna o valor atual de <code>intensity</code>.</>,
    },
    {
      signature: "getMode()",
      returns: "Mode",
      description: <>Retorna o valor atual de <code>mode</code>.</>,
    },
    {
      signature: "setBorder(float)",
      returns: "void",
      description: <>Define <code>border</code>.</>,
    },
    {
      signature: "setCenter(Vector2)",
      returns: "void",
      description: <>Define <code>center</code>.</>,
    },
    {
      signature: "setIntensity(float)",
      returns: "void",
      description: <>Define <code>intensity</code>.</>,
    },
    {
      signature: "setMode(Mode)",
      returns: "void",
      description: <>Define <code>mode</code>.</>,
    }
  ]}
/>
