import ApiDoc from '@site/src/components/ApiDoc';

# UIVerticalLayout

Referência completa da API de `UIVerticalLayout`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIVerticalLayout extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIVerticalLayout()",
      description: <>Cria uma nova inst?ncia de UIVerticalLayout.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "spacing",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSpacing</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getSpacing()",
      returns: "int",
      description: <>Retorna o valor atual de <code>spacing</code>.</>,
    },
    {
      signature: "setSpacing(int)",
      returns: "void",
      description: <>Define <code>spacing</code>.</>,
    }
  ]}
/>
