import ApiDoc from '@site/src/components/ApiDoc';

# UIWebView

Referência completa da API de `UIWebView`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIWebView extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIWebView()",
      description: <>Cria uma nova inst?ncia de UIWebView.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "url",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getUrl</code>. Acesse como um campo para ler ou escrever o valor interno.</>
    },
    {
      name: "fps",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getFps</code>. Acesse como um campo para ler ou escrever o valor interno.</>
    },
    {
      name: "enableVirtualKeyboard",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>isEnableVirtualKeyboard</code>. Acesse como um campo para ler ou escrever o valor interno.</>
    },
    {
      name: "allowHibernate",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>isAllowHibernate</code>. Acesse como um campo para ler ou escrever o valor interno.</>
    },
    {
      name: "texture",
      type: "Texture",
      access: "get",
      description: <>Atributo virtual exposto por <code>getTexture</code>. Acesse como um campo para ler o valor interno.</>
    },
    {
      name: "loadedUrl",
      type: "String",
      access: "get",
      description: <>Atributo virtual exposto por <code>getLoadedUrl</code>. Acesse como um campo para ler o valor interno.</>
    }
  ]}
  methods={[
    {
      signature: "getUrl()",
      returns: "String",
      description: <>Retorna o valor atual de <code>url</code>.</>,
    },
    {
      signature: "getFps()",
      returns: "int",
      description: <>Retorna o valor atual de <code>fps</code>.</>,
    },
    {
      signature: "isEnableVirtualKeyboard()",
      returns: "boolean",
      description: <>Retorna se <code>enableVirtualKeyboard</code> est? ativado.</>,
    },
    {
      signature: "isAllowHibernate()",
      returns: "boolean",
      description: <>Retorna se <code>allowHibernate</code> est? ativado.</>,
    },
    {
      signature: "getTexture()",
      returns: "Texture",
      description: <>Retorna o valor atual de <code>texture</code>.</>,
    },
    {
      signature: "getLoadedUrl()",
      returns: "String",
      description: <>Retorna o valor atual de <code>loadedUrl</code>.</>,
    },
    {
      signature: "setUrl(String)",
      returns: "void",
      description: <>Define <code>url</code>.</>,
    },
    {
      signature: "setFps(int)",
      returns: "void",
      description: <>Define <code>fps</code>.</>,
    },
    {
      signature: "setEnableVirtualKeyboard(boolean)",
      returns: "void",
      description: <>Define <code>enableVirtualKeyboard</code>.</>,
    },
    {
      signature: "setAllowHibernate(boolean)",
      returns: "void",
      description: <>Define <code>allowHibernate</code>.</>,
    }
  ]}
/>
