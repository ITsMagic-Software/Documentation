import ApiDoc from '@site/src/components/ApiDoc';

# UIContrastBrightnessSaturation

Referência completa da API de `UIContrastBrightnessSaturation`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class UIContrastBrightnessSaturation extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIContrastBrightnessSaturation()",
      description: <>Cria uma nova inst?ncia de UIContrastBrightnessSaturation.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "brightness",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getBrightness</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "contrast",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getContrast</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "saturation",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSaturation</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getBrightness()",
      returns: "float",
      description: <>Retorna o valor atual de <code>brightness</code>.</>,
    },
    {
      signature: "getContrast()",
      returns: "float",
      description: <>Retorna o valor atual de <code>contrast</code>.</>,
    },
    {
      signature: "getSaturation()",
      returns: "float",
      description: <>Retorna o valor atual de <code>saturation</code>.</>,
    },
    {
      signature: "setBrightness(float)",
      returns: "void",
      description: <>Define <code>brightness</code>.</>,
    },
    {
      signature: "setContrast(float)",
      returns: "void",
      description: <>Define <code>contrast</code>.</>,
    },
    {
      signature: "setSaturation(float)",
      returns: "void",
      description: <>Define <code>saturation</code>.</>,
    }
  ]}
/>
