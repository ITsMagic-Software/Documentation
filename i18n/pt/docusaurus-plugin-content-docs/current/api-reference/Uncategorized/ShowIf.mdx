import ApiDoc from '@site/src/components/ApiDoc';

# ShowIf

Refer?ncia completa da API de `ShowIf`, incluindo construtores, atributos, atributos virtuais e m?todos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** n?o

---

```java
public interface ShowIf
```

<ApiDoc
  methods={[
    {
      signature: "value()",
      returns: "String",
      description: <>Executa <code>value</code> e retorna quando a chamada do motor termina.</>,
    },
    {
      signature: "isTrue()",
      returns: "boolean",
      description: <>Retorna se <code>true</code> est? ativado.</>,
    },
    {
      signature: "enumValue()",
      returns: "String",
      description: <>Executa <code>enumValue</code> e retorna quando a chamada do motor termina.</>,
    }
  ]}
/>
