import ApiDoc from '@site/src/components/ApiDoc';

# BakeChildSuppressor

Refer?ncia completa da API de `BakeChildSuppressor`, incluindo construtores, atributos, atributos virtuais e m?todos.

**Pacote:** `JAVARuntime`

**Categoria:** Components

**Componente:** sim

---

```java
public class BakeChildSuppressor extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "BakeChildSuppressor(BakeChildSuppressor)",
      description: <>Cria uma nova inst?ncia de BakeChildSuppressor.</>,
    },
    {
      signature: "BakeChildSuppressor(Engine)",
      description: <>Cria uma nova inst?ncia de BakeChildSuppressor.</>,
    },
    {
      signature: "BakeChildSuppressor()",
      description: <>Cria uma nova inst?ncia de BakeChildSuppressor.</>,
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "BakeChildSuppressor",
      description: <>Representa o valor de <code>instance</code> armazenado nesta inst?ncia. Leia ou atribua para atualizar a propriedade.</>,
    }
  ]}
/>
