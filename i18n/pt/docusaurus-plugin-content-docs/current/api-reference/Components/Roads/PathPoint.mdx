import ApiDoc from '@site/src/components/ApiDoc';

# PathPoint

Refer?ncia completa da API de `PathPoint`, incluindo construtores, atributos, atributos virtuais e m?todos.

**Pacote:** `JAVARuntime`

**Categoria:** Roads

**Componente:** sim

---

```java
public class PathPoint extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "PathPoint(PathPoint)",
      description: <>Cria uma nova inst?ncia de PathPoint.</>,
    },
    {
      signature: "PathPoint(Engine)",
      description: <>Cria uma nova inst?ncia de PathPoint.</>,
    },
    {
      signature: "PathPoint()",
      description: <>Cria uma nova inst?ncia de PathPoint.</>,
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "PathPoint",
      description: <>Representa o valor de <code>instance</code> armazenado nesta inst?ncia. Leia ou atribua para atualizar a propriedade.</>,
    }
  ]}
/>
