import ApiDoc from '@site/src/components/ApiDoc';

# ModelTerrainLevel

Refer?ncia completa da API de `ModelTerrainLevel`, incluindo construtores, atributos, atributos virtuais e m?todos.

**Pacote:** `JAVARuntime`

**Categoria:** Terrain

**Componente:** sim

---

```java
public class ModelTerrainLevel extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "ModelTerrainLevel(ModelTerrainLevel)",
      description: <>Cria uma nova inst?ncia de ModelTerrainLevel.</>,
    },
    {
      signature: "ModelTerrainLevel(Engine)",
      description: <>Cria uma nova inst?ncia de ModelTerrainLevel.</>,
    },
    {
      signature: "ModelTerrainLevel()",
      description: <>Cria uma nova inst?ncia de ModelTerrainLevel.</>,
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "ModelTerrainLevel",
      description: <>Representa o valor de <code>instance</code> armazenado nesta inst?ncia. Leia ou atribua para atualizar a propriedade.</>,
    }
  ]}
  virtualAttributes={[
    {
      name: "verticalOffset",
      type: "float",
      access: "get/set",
      description: <>Retorna o valor atual de <code>vertical offset</code>.</>,
    }
  ]}
/>
