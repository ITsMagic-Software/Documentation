import ApiDoc from '@site/src/components/ApiDoc';

# VSMShadow

Referencia completa da API de `VSMShadow`, incluindo construtores, atributos, atributos virtuais e metodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class VSMShadow extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "VSMShadow(VSMShadow)",
      description: <>Executa <code>VSMShadow</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "VSMShadow(Engine)",
      description: <>Executa <code>VSMShadow</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "VSMShadow()",
      description: <>Executa <code>VSMShadow</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "VSMShadow",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instancia. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "anisotropy",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAnisotropy</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "mipmapping",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>isMipmapping</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "msaaSamples",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMsaaSamples</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "highPrecision",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>isHighPrecision</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "minVarianceScale",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMinVarianceScale</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "lightBleedReduction",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getLightBleedReduction</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
