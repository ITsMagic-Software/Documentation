import ApiDoc from '@site/src/components/ApiDoc';

# DPCFShadow

Referencia completa da API de `DPCFShadow`, incluindo construtores, atributos, atributos virtuais e metodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class DPCFShadow extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "DPCFShadow(DPCFShadow)",
      description: <>Executa <code>DPCFShadow</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "DPCFShadow(Engine)",
      description: <>Executa <code>DPCFShadow</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "DPCFShadow()",
      description: <>Executa <code>DPCFShadow</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "DPCFShadow",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instancia. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "penumbraScale",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getPenumbraScale</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "penumbraRatioScale",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getPenumbraRatioScale</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
