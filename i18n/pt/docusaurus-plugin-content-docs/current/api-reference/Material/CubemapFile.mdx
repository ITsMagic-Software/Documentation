import ApiDoc from '@site/src/components/ApiDoc';

# CubemapFile

Referência completa da API de `CubemapFile`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Material

**Componente:** não

---

```java
public class CubemapFile
```

<ApiDoc
  constructors={[
    {
      signature: "CubemapFile(String)",
      description: <>Executa <code>CubemapFile</code> e retorna depois que a chamada interna da engine termina.</>
    },
    {
      signature: "CubemapFile()",
      description: <>Executa <code>CubemapFile</code> e retorna depois que a chamada interna da engine termina.</>
    }
  ]}
  methods={[
    {
      signature: "toString()",
      returns: "String",
      description: <>Retorna a representação em texto.</>
    }
  ]}
  staticMethods={[
    {
      signature: "genericInterface()",
      returns: "GenericClassInspectorEntry",
      description: <>Executa <code>genericInterface</code> e retorna depois que a chamada interna da engine termina.</>
    },
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executa <code>inspectorController</code> e retorna depois que a chamada interna da engine termina.</>
    }
  ]}
/>
