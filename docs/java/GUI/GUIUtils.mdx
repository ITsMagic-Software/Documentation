import ApiDoc from '@site/src/components/ApiDoc';

# GUIUtils

Complete API reference for `GUIUtils`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** GUI

**Component:** no

---

```java
public class GUIUtils
```

<ApiDoc
  staticMethods={[
    {
      signature: "applyFilter(com.itsmagic.engine.Engines.Input.VOS.Touch, TouchFilter)",
      returns: "Object",
      description: <>Returns the result of <code>applyFilter</code>.</>
    },
    {
      signature: "isTouchInside(Touch, int, int, int, int)",
      returns: "boolean",
      description: <>Returns whether touch inside is true.</>
    },
    {
      signature: "determineTouch(int, int, int, int)",
      returns: "Touch",
      description: <>Performs the determineTouch operation using the provided parameters (x, y, width, height).</>
    },
    {
      signature: "determineTouch(int, int, int, int, TouchFilter)",
      returns: "Touch",
      description: <>Performs the determineTouch operation using the provided parameters (x, y, width, height, touchFilter).</>
    },
    {
      signature: "determineTouches(int, int, int, int)",
      returns: "Touch[]",
      description: <>Performs the determineTouches operation using the provided parameters (x, y, width, height).</>
    },
    {
      signature: "determineTouches(int, int, int, int, TouchFilter)",
      returns: "Touch[]",
      description: <>Performs the determineTouches operation using the provided parameters (x, y, width, height, touchFilter).</>
    },
    {
      signature: "determineTouchList(int, int, int, int)",
      returns: "List&lt;Touch&gt;",
      description: <>Performs the determineTouchList operation using the provided parameters (x, y, width, height).</>
    },
    {
      signature: "determineTouchList(int, int, int, int, TouchFilter)",
      returns: "List&lt;Touch&gt;",
      description: <>Performs the determineTouchList operation using the provided parameters (x, y, width, height, touchFilter).</>
    }
  ]}
/>
