import ApiDoc from '@site/src/components/ApiDoc';

# ParticleGroup

Complete API reference for `ParticleGroup`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Particles

**Component:** yes

---

```java
public class ParticleGroup extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "ParticleGroup(ParticleGroup)",
      description: <>Creates a new instance of ParticleGroup.</>,
    },
    {
      signature: "ParticleGroup(Engine)",
      description: <>Creates a new instance of ParticleGroup.</>,
    },
    {
      signature: "ParticleGroup()",
      description: <>Creates a new instance of ParticleGroup.</>,
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "ParticleGroup",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>,
    }
  ]}
/>
