import ApiDoc from '@site/src/components/ApiDoc';

# PathPoint

Complete API reference for `PathPoint`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Roads

**Component:** yes

---

```java
public class PathPoint extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "PathPoint(PathPoint)",
      description: <>Creates a new instance of PathPoint.</>,
    },
    {
      signature: "PathPoint(Engine)",
      description: <>Creates a new instance of PathPoint.</>,
    },
    {
      signature: "PathPoint()",
      description: <>Creates a new instance of PathPoint.</>,
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "PathPoint",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>,
    }
  ]}
/>
