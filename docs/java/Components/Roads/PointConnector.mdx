import ApiDoc from '@site/src/components/ApiDoc';

# PointConnector

Complete API reference for `PointConnector`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Roads

**Component:** yes

---

```java
public class PointConnector extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "PointConnector(PointConnector)",
      description: <>Creates a new instance of PointConnector.</>,
    },
    {
      signature: "PointConnector(Engine)",
      description: <>Creates a new instance of PointConnector.</>,
    },
    {
      signature: "PointConnector()",
      description: <>Creates a new instance of PointConnector.</>,
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "PointConnector",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>,
    }
  ]}
/>
