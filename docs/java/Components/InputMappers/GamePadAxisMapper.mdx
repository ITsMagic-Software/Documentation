import ApiDoc from '@site/src/components/ApiDoc';

# GamePadAxisMapper

Complete API reference for `GamePadAxisMapper`, including constructors, attributes, and virtual attributes.

**Package:** `JAVARuntime`

**Category:** Input Mappers

**Component:** yes

---

```java
public class GamePadAxisMapper extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "GamePadAxisMapper()",
      description: <>Creates a new instance of GamePadAxisMapper.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "GamePadAxisMapper",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "gamepadAxis",
      type: "Axis",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getGamepadAxis</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "axisType",
      type: "AxisType",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAxisType</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "outputAxis",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getOutputAxis</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
