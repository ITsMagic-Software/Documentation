import ApiDoc from '@site/src/components/ApiDoc';

# MouseAxisMapper

Complete API reference for `MouseAxisMapper`, including constructors, attributes, and virtual attributes.

**Package:** `JAVARuntime`

**Category:** Input Mappers

**Component:** yes

---

```java
public class MouseAxisMapper extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "MouseAxisMapper()",
      description: <>Creates a new instance of MouseAxisMapper.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "MouseAxisMapper",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "outputAxis",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getOutputAxis</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
