import ApiDoc from '@site/src/components/ApiDoc';

# KeyboardAxisMapper

Complete API reference for `KeyboardAxisMapper`, including constructors, attributes, and virtual attributes.

**Package:** `JAVARuntime`

**Category:** Input Mappers

**Component:** yes

---

```java
public class KeyboardAxisMapper extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "KeyboardAxisMapper()",
      description: <>Creates a new instance of KeyboardAxisMapper.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "KeyboardAxisMapper",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "negativeKeyboardKey",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getNegativeKeyboardKey</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "positiveKeyboardKey",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getPositiveKeyboardKey</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "axisType",
      type: "AxisType",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAxisType</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "outputAxis",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getOutputAxis</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
