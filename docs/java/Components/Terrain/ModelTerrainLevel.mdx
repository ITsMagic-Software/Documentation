import ApiDoc from '@site/src/components/ApiDoc';

# ModelTerrainLevel

Complete API reference for `ModelTerrainLevel`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Terrain

**Component:** yes

---

```java
public class ModelTerrainLevel extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "ModelTerrainLevel(ModelTerrainLevel)",
      description: <>Creates a new instance of ModelTerrainLevel.</>,
    },
    {
      signature: "ModelTerrainLevel(Engine)",
      description: <>Creates a new instance of ModelTerrainLevel.</>,
    },
    {
      signature: "ModelTerrainLevel()",
      description: <>Creates a new instance of ModelTerrainLevel.</>,
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "ModelTerrainLevel",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>,
    }
  ]}
  virtualAttributes={[
    {
      name: "verticalOffset",
      type: "float",
      access: "get/set",
      description: <>Returns the current vertical offset.</>,
    }
  ]}
/>
