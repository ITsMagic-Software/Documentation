import ApiDoc from '@site/src/components/ApiDoc';

# StickToTerrain

Complete API reference for `StickToTerrain`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Terrain

**Component:** yes

---

```java
public class StickToTerrain extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "StickToTerrain(StickToTerrain)",
      description: <>Creates a new instance of StickToTerrain.</>,
    },
    {
      signature: "StickToTerrain(Engine)",
      description: <>Creates a new instance of StickToTerrain.</>,
    },
    {
      signature: "StickToTerrain()",
      description: <>Creates a new instance of StickToTerrain.</>,
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "StickToTerrain",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>,
    }
  ]}
/>
