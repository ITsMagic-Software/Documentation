import ApiDoc from '@site/src/components/ApiDoc';

# UICustomPostProcessing

Complete API reference for `UICustomPostProcessing`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class UICustomPostProcessing extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UICustomPostProcessing(UICustomPostProcessing)",
      description: <>Executes <code>UICustomPostProcessing</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UICustomPostProcessing(Engine)",
      description: <>Executes <code>UICustomPostProcessing</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UICustomPostProcessing()",
      description: <>Executes <code>UICustomPostProcessing</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UICustomPostProcessing",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "shaderFile",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getShaderFile</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "time",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTime</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "setParam(String, float)",
      returns: "void",
      description: <>Executes <code>setParam</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setParam(String, Color)",
      returns: "void",
      description: <>Executes <code>setParam</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setParam(String, Texture)",
      returns: "void",
      description: <>Executes <code>setParam</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setParam(String, Object)",
      returns: "void",
      description: <>Executes <code>setParam</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setParam(String, float[])",
      returns: "void",
      description: <>Executes <code>setParam</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
