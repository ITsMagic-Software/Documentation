import ApiDoc from '@site/src/components/ApiDoc';

# Skybox

Complete API reference for `Skybox`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class Skybox extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "Skybox(Skybox)",
      description: <>Executes <code>Skybox</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Skybox(Engine)",
      description: <>Executes <code>Skybox</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Skybox()",
      description: <>Executes <code>Skybox</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "Skybox",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "lux",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getLux</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "maxResolutionID",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMaxResolutionID</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "showSun",
      type: "boolean",
      access: "set",
      description: <>Virtual attribute exposed by <code>setShowSun</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "cubemapFile",
      type: "CubemapFile",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getCubemapFile</code>. Use a <code>CubemapFile</code> to read or replace the skybox project cubemap file.</>
    }
  ]}
  methods={[
    {
      signature: "isShowSun()",
      returns: "boolean",
      description: <>Executes <code>isShowSun</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setCubemapFile(String)",
      returns: "void",
      description: <>Sets the cubemap from a raw project path string when you are not using a <code>CubemapFile</code> instance.</>
    }
  ]}
/>
