import ApiDoc from '@site/src/components/ApiDoc';

# PoolSearchResult

Complete API reference for `PoolSearchResult`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public class PoolSearchResult
```

<ApiDoc
  constructors={[
    {
      signature: "PoolSearchResult(SearchResult)",
      description: <>Creates a new instance of PoolSearchResult.</>,
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "SearchResult",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>,
    }
  ]}
  virtualAttributes={[
    {
      name: "distance",
      type: "float",
      access: "get",
      description: <>Returns the distance of the HPOP search result.</>,
    },
    {
      name: "object",
      type: "PooledObject",
      access: "get",
      description: <>Returns the object of the HPOP search result.</>,
    },
    {
      name: "squaredDistance",
      type: "float",
      access: "get",
      description: <>Returns the squared distance of the HPOP search result.</>,
    }
  ]}
/>
