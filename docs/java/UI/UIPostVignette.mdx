import ApiDoc from '@site/src/components/ApiDoc';

# UIPostVignette

Complete API reference for `UIPostVignette`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIPostVignette extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIPostVignette()",
      description: <>Creates a new instance of UIPostVignette.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "feather",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getFeather</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "midPoint",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMidPoint</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "roundness",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRoundness</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "getColor()",
      returns: "Color",
      description: <>Returns the current <code>color</code> value.</>,
    },
    {
      signature: "getFeather()",
      returns: "float",
      description: <>Returns the current <code>feather</code> value.</>,
    },
    {
      signature: "getMidPoint()",
      returns: "float",
      description: <>Returns the current <code>midPoint</code> value.</>,
    },
    {
      signature: "getRoundness()",
      returns: "float",
      description: <>Returns the current <code>roundness</code> value.</>,
    },
    {
      signature: "setColor(Color)",
      returns: "void",
      description: <>Sets <code>color</code>.</>,
    },
    {
      signature: "setFeather(float)",
      returns: "void",
      description: <>Sets <code>feather</code>.</>,
    },
    {
      signature: "setMidPoint(float)",
      returns: "void",
      description: <>Sets <code>midPoint</code>.</>,
    },
    {
      signature: "setRoundness(float)",
      returns: "void",
      description: <>Sets <code>roundness</code>.</>,
    }
  ]}
/>
