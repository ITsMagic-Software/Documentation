import ApiDoc from '@site/src/components/ApiDoc';

# UIButton

Complete API reference for `UIButton`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIButton extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIButton()",
      description: <>Creates a new instance of UIButton.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "border",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getBorder</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "down",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isDown</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "fontFile",
      type: "String",
      access: "get",
      description: <>Virtual attribute exposed by <code>getFontFile</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "normalColor",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getNormalColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "normalTexture",
      type: "Texture",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getNormalTexture</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "padding",
      type: "Vector2",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getPadding</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "pressed",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isPressed</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "pressedColor",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getPressedColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "pressedTexture",
      type: "Texture",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getPressedTexture</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "resolution",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getResolution</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "text",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getText</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "textNormalColor",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTextNormalColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "textPressedColor",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTextPressedColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "textSize",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTextSize</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "up",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isUp</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "getBorder()",
      returns: "float",
      description: <>Returns the current <code>border</code> value.</>,
    },
    {
      signature: "getFontFile()",
      returns: "String",
      description: <>Returns the current <code>fontFile</code> value.</>,
    },
    {
      signature: "getNormalColor()",
      returns: "Color",
      description: <>Returns the current <code>normalColor</code> value.</>,
    },
    {
      signature: "getNormalTexture()",
      returns: "Texture",
      description: <>Returns the current <code>normalTexture</code> value.</>,
    },
    {
      signature: "getPadding()",
      returns: "Vector2",
      description: <>Returns the current <code>padding</code> value.</>,
    },
    {
      signature: "getPressedColor()",
      returns: "Color",
      description: <>Returns the current <code>pressedColor</code> value.</>,
    },
    {
      signature: "getPressedTexture()",
      returns: "Texture",
      description: <>Returns the current <code>pressedTexture</code> value.</>,
    },
    {
      signature: "getResolution()",
      returns: "int",
      description: <>Returns the current <code>resolution</code> value.</>,
    },
    {
      signature: "getText()",
      returns: "String",
      description: <>Returns the current <code>text</code> value.</>,
    },
    {
      signature: "getTextNormalColor()",
      returns: "Color",
      description: <>Returns the current <code>textNormalColor</code> value.</>,
    },
    {
      signature: "getTextPressedColor()",
      returns: "Color",
      description: <>Returns the current <code>textPressedColor</code> value.</>,
    },
    {
      signature: "getTextSize()",
      returns: "float",
      description: <>Returns the current <code>textSize</code> value.</>,
    },
    {
      signature: "isDown()",
      returns: "boolean",
      description: <>Returns whether <code>down</code> is true.</>,
    },
    {
      signature: "isPressed()",
      returns: "boolean",
      description: <>Returns whether <code>pressed</code> is true.</>,
    },
    {
      signature: "isUp()",
      returns: "boolean",
      description: <>Returns whether <code>up</code> is true.</>,
    },
    {
      signature: "setBorder(float)",
      returns: "void",
      description: <>Sets <code>border</code>.</>,
    },
    {
      signature: "setNormalColor(Color)",
      returns: "void",
      description: <>Sets <code>normalColor</code>.</>,
    },
    {
      signature: "setNormalTexture(Texture)",
      returns: "void",
      description: <>Sets <code>normalTexture</code>.</>,
    },
    {
      signature: "setPadding(Vector2)",
      returns: "void",
      description: <>Sets <code>padding</code>.</>,
    },
    {
      signature: "setPressedColor(Color)",
      returns: "void",
      description: <>Sets <code>pressedColor</code>.</>,
    },
    {
      signature: "setPressedTexture(Texture)",
      returns: "void",
      description: <>Sets <code>pressedTexture</code>.</>,
    },
    {
      signature: "setResolution(int)",
      returns: "void",
      description: <>Sets <code>resolution</code>.</>,
    },
    {
      signature: "setText(String)",
      returns: "void",
      description: <>Sets <code>text</code>.</>,
    },
    {
      signature: "setTextNormalColor(Color)",
      returns: "void",
      description: <>Sets <code>textNormalColor</code>.</>,
    },
    {
      signature: "setTextPressedColor(Color)",
      returns: "void",
      description: <>Sets <code>textPressedColor</code>.</>,
    },
    {
      signature: "setTextSize(float)",
      returns: "void",
      description: <>Sets <code>textSize</code>.</>,
    }
  ]}
/>
