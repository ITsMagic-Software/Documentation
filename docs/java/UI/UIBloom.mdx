import ApiDoc from '@site/src/components/ApiDoc';

# UIBloom

Complete API reference for `UIBloom`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIBloom extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIBloom()",
      description: <>Creates a new instance of UIBloom.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "blurRadius",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getBlurRadius</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "intensity",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getIntensity</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "quality",
      type: "Quality",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getQuality</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "threshold",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getThreshold</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "getIntensity()",
      returns: "float",
      description: <>Returns the current <code>intensity</code> value.</>,
    },
    {
      signature: "getThreshold()",
      returns: "float",
      description: <>Returns the current <code>threshold</code> value.</>,
    },
    {
      signature: "getBlurRadius()",
      returns: "float",
      description: <>Returns the current <code>blurRadius</code> value.</>,
    },
    {
      signature: "getQuality()",
      returns: "Quality",
      description: <>Returns the current <code>quality</code> value.</>,
    },
    {
      signature: "setIntensity(float)",
      returns: "void",
      description: <>Sets <code>intensity</code>.</>,
    },
    {
      signature: "setThreshold(float)",
      returns: "void",
      description: <>Sets <code>threshold</code>.</>,
    },
    {
      signature: "setBlurRadius(float)",
      returns: "void",
      description: <>Sets <code>blurRadius</code>.</>,
    },
    {
      signature: "setQuality(Quality)",
      returns: "void",
      description: <>Sets <code>quality</code>.</>,
    }
  ]}
/>
