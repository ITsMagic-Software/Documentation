import ApiDoc from '@site/src/components/ApiDoc';

# UIRotateImage

Complete API reference for `UIRotateImage`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIRotateImage extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIRotateImage()",
      description: <>Creates a new instance of UIRotateImage.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "angle",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAngle</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "texture",
      type: "Texture",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTexture</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "getAngle()",
      returns: "float",
      description: <>Returns the current <code>angle</code> value.</>,
    },
    {
      signature: "setAngle(float)",
      returns: "void",
      description: <>Sets <code>angle</code>.</>,
    },
    {
      signature: "getTexture()",
      returns: "Texture",
      description: <>Returns the current <code>texture</code> value.</>,
    },
    {
      signature: "setTexture(Texture)",
      returns: "void",
      description: <>Sets <code>texture</code>.</>,
    }
  ]}
/>
