import ApiDoc from '@site/src/components/ApiDoc';

# UIDrivingWheel

Complete API reference for `UIDrivingWheel`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIDrivingWheel extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIDrivingWheel()",
      description: <>Creates a new instance of UIDrivingWheel.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "value",
      type: "float",
      access: "get",
      description: <>Virtual attribute exposed by <code>getValue</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "angle",
      type: "float",
      access: "get",
      description: <>Virtual attribute exposed by <code>getAngle</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "lerp",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getLerp</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "maxAngle",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMaxAngle</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "pressed",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isPressed</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "down",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isDown</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "up",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isUp</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "texture",
      type: "Texture",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTexture</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "getValue()",
      returns: "float",
      description: <>Returns the current <code>value</code> value.</>,
    },
    {
      signature: "getAngle()",
      returns: "float",
      description: <>Returns the current <code>angle</code> value.</>,
    },
    {
      signature: "getLerp()",
      returns: "float",
      description: <>Returns the current <code>lerp</code> value.</>,
    },
    {
      signature: "setLerp(float)",
      returns: "void",
      description: <>Sets <code>lerp</code>.</>,
    },
    {
      signature: "getMaxAngle()",
      returns: "float",
      description: <>Returns the current <code>maxAngle</code> value.</>,
    },
    {
      signature: "setMaxAngle(float)",
      returns: "void",
      description: <>Sets <code>maxAngle</code>.</>,
    },
    {
      signature: "isPressed()",
      returns: "boolean",
      description: <>Returns whether <code>pressed</code> is true.</>,
    },
    {
      signature: "isDown()",
      returns: "boolean",
      description: <>Returns whether <code>down</code> is true.</>,
    },
    {
      signature: "isUp()",
      returns: "boolean",
      description: <>Returns whether <code>up</code> is true.</>,
    },
    {
      signature: "getColor()",
      returns: "Color",
      description: <>Returns the current <code>color</code> value.</>,
    },
    {
      signature: "getTexture()",
      returns: "Texture",
      description: <>Returns the current <code>texture</code> value.</>,
    },
    {
      signature: "setColor(Color)",
      returns: "void",
      description: <>Sets <code>color</code>.</>,
    },
    {
      signature: "setTexture(Texture)",
      returns: "void",
      description: <>Sets <code>texture</code>.</>,
    }
  ]}
/>
