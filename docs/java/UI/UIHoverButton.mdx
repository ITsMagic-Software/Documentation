import ApiDoc from '@site/src/components/ApiDoc';

# UIHoverButton

Complete API reference for `UIHoverButton`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIHoverButton extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIHoverButton()",
      description: <>Creates a new instance of UIHoverButton.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "border",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getBorder</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "clicked",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isClicked</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "clickedColor",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getClickedColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "clickedTexture",
      type: "Texture",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getClickedTexture</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "fontFile",
      type: "String",
      access: "get",
      description: <>Virtual attribute exposed by <code>getFontFile</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "hover",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isHover</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "hoverColor",
      type: "Color",
      access: "set",
      description: <>Virtual attribute exposed by <code>setHoverColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "hoverTexture",
      type: "Texture",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getHoverTexture</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "justBeginHover",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isJustBeginHover</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "normalColor",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getNormalColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "normalTexture",
      type: "Texture",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getNormalTexture</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "padding",
      type: "Vector2",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getPadding</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "resolution",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getResolution</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "text",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getText</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "textClickedColor",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTextClickedColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "textHoverColor",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTextHoverColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "textNormalColor",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTextNormalColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "textSize",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTextSize</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "getBorder()",
      returns: "float",
      description: <>Returns the current <code>border</code> value.</>,
    },
    {
      signature: "getFontFile()",
      returns: "String",
      description: <>Returns the current <code>fontFile</code> value.</>,
    },
    {
      signature: "getNormalColor()",
      returns: "Color",
      description: <>Returns the current <code>normalColor</code> value.</>,
    },
    {
      signature: "getNormalTexture()",
      returns: "Texture",
      description: <>Returns the current <code>normalTexture</code> value.</>,
    },
    {
      signature: "getHoverTexture()",
      returns: "Texture",
      description: <>Returns the current <code>hoverTexture</code> value.</>,
    },
    {
      signature: "getPadding()",
      returns: "Vector2",
      description: <>Returns the current <code>padding</code> value.</>,
    },
    {
      signature: "getClickedColor()",
      returns: "Color",
      description: <>Returns the current <code>clickedColor</code> value.</>,
    },
    {
      signature: "getClickedTexture()",
      returns: "Texture",
      description: <>Returns the current <code>clickedTexture</code> value.</>,
    },
    {
      signature: "getResolution()",
      returns: "int",
      description: <>Returns the current <code>resolution</code> value.</>,
    },
    {
      signature: "getText()",
      returns: "String",
      description: <>Returns the current <code>text</code> value.</>,
    },
    {
      signature: "getTextNormalColor()",
      returns: "Color",
      description: <>Returns the current <code>textNormalColor</code> value.</>,
    },
    {
      signature: "getTextHoverColor()",
      returns: "Color",
      description: <>Returns the current <code>textHoverColor</code> value.</>,
    },
    {
      signature: "getTextClickedColor()",
      returns: "Color",
      description: <>Returns the current <code>textClickedColor</code> value.</>,
    },
    {
      signature: "getTextSize()",
      returns: "float",
      description: <>Returns the current <code>textSize</code> value.</>,
    },
    {
      signature: "isJustBeginHover()",
      returns: "boolean",
      description: <>Returns whether <code>justBeginHover</code> is true.</>,
    },
    {
      signature: "isClicked()",
      returns: "boolean",
      description: <>Returns whether <code>clicked</code> is true.</>,
    },
    {
      signature: "isHover()",
      returns: "boolean",
      description: <>Returns whether <code>hover</code> is true.</>,
    },
    {
      signature: "setBorder(float)",
      returns: "void",
      description: <>Sets <code>border</code>.</>,
    },
    {
      signature: "setNormalColor(Color)",
      returns: "void",
      description: <>Sets <code>normalColor</code>.</>,
    },
    {
      signature: "setNormalTexture(Texture)",
      returns: "void",
      description: <>Sets <code>normalTexture</code>.</>,
    },
    {
      signature: "setHoverTexture(Texture)",
      returns: "void",
      description: <>Sets <code>hoverTexture</code>.</>,
    },
    {
      signature: "setPadding(Vector2)",
      returns: "void",
      description: <>Sets <code>padding</code>.</>,
    },
    {
      signature: "setClickedColor(Color)",
      returns: "void",
      description: <>Sets <code>clickedColor</code>.</>,
    },
    {
      signature: "setHoverColor(Color)",
      returns: "void",
      description: <>Sets <code>hoverColor</code>.</>,
    },
    {
      signature: "setClickedTexture(Texture)",
      returns: "void",
      description: <>Sets <code>clickedTexture</code>.</>,
    },
    {
      signature: "setResolution(int)",
      returns: "void",
      description: <>Sets <code>resolution</code>.</>,
    },
    {
      signature: "setText(String)",
      returns: "void",
      description: <>Sets <code>text</code>.</>,
    },
    {
      signature: "setTextNormalColor(Color)",
      returns: "void",
      description: <>Sets <code>textNormalColor</code>.</>,
    },
    {
      signature: "setTextHoverColor(Color)",
      returns: "void",
      description: <>Sets <code>textHoverColor</code>.</>,
    },
    {
      signature: "setTextClickedColor(Color)",
      returns: "void",
      description: <>Sets <code>textClickedColor</code>.</>,
    },
    {
      signature: "setTextSize(float)",
      returns: "void",
      description: <>Sets <code>textSize</code>.</>,
    }
  ]}
/>
