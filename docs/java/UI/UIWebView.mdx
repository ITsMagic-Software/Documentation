import ApiDoc from '@site/src/components/ApiDoc';

# UIWebView

Complete API reference for `UIWebView`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIWebView extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIWebView()",
      description: <>Creates a new instance of UIWebView.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "url",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getUrl</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "fps",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getFps</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "enableVirtualKeyboard",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isEnableVirtualKeyboard</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "allowHibernate",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isAllowHibernate</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "texture",
      type: "Texture",
      access: "get",
      description: <>Virtual attribute exposed by <code>getTexture</code>. Access it like a field to read the underlying value.</>
    },
    {
      name: "loadedUrl",
      type: "String",
      access: "get",
      description: <>Virtual attribute exposed by <code>getLoadedUrl</code>. Access it like a field to read the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "getUrl()",
      returns: "String",
      description: <>Returns the current <code>url</code> value.</>,
    },
    {
      signature: "getFps()",
      returns: "int",
      description: <>Returns the current <code>fps</code> value.</>,
    },
    {
      signature: "isEnableVirtualKeyboard()",
      returns: "boolean",
      description: <>Returns whether <code>enableVirtualKeyboard</code> is true.</>,
    },
    {
      signature: "isAllowHibernate()",
      returns: "boolean",
      description: <>Returns whether <code>allowHibernate</code> is true.</>,
    },
    {
      signature: "getTexture()",
      returns: "Texture",
      description: <>Returns the current <code>texture</code> value.</>,
    },
    {
      signature: "getLoadedUrl()",
      returns: "String",
      description: <>Returns the current <code>loadedUrl</code> value.</>,
    },
    {
      signature: "setUrl(String)",
      returns: "void",
      description: <>Sets <code>url</code>.</>,
    },
    {
      signature: "setFps(int)",
      returns: "void",
      description: <>Sets <code>fps</code>.</>,
    },
    {
      signature: "setEnableVirtualKeyboard(boolean)",
      returns: "void",
      description: <>Sets <code>enableVirtualKeyboard</code>.</>,
    },
    {
      signature: "setAllowHibernate(boolean)",
      returns: "void",
      description: <>Sets <code>allowHibernate</code>.</>,
    }
  ]}
/>
