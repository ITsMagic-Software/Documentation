import ApiDoc from '@site/src/components/ApiDoc';

# UIFitParent

Complete API reference for `UIFitParent`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIFitParent extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIFitParent()",
      description: <>Creates a new instance of UIFitParent.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "fitHeight",
      type: "boolean",
      access: "set",
      description: <>Virtual attribute exposed by <code>setFitHeight</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "fitWidth",
      type: "boolean",
      access: "set",
      description: <>Virtual attribute exposed by <code>setFitWidth</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "isFitHeight()",
      returns: "boolean",
      description: <>Returns whether <code>fitHeight</code> is true.</>,
    },
    {
      signature: "isFitWidth()",
      returns: "boolean",
      description: <>Returns whether <code>fitWidth</code> is true.</>,
    },
    {
      signature: "setFitHeight(boolean)",
      returns: "void",
      description: <>Sets <code>fitHeight</code>.</>,
    },
    {
      signature: "setFitWidth(boolean)",
      returns: "void",
      description: <>Sets <code>fitWidth</code>.</>,
    }
  ]}
/>
