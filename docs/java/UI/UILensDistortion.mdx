import ApiDoc from '@site/src/components/ApiDoc';

# UILensDistortion

Complete API reference for `UILensDistortion`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UILensDistortion extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UILensDistortion()",
      description: <>Creates a new instance of UILensDistortion.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "border",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getBorder</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "center",
      type: "Vector2",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getCenter</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "intensity",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getIntensity</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "mode",
      type: "Mode",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMode</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "getBorder()",
      returns: "float",
      description: <>Returns the current <code>border</code> value.</>,
    },
    {
      signature: "getCenter()",
      returns: "Vector2",
      description: <>Returns the current <code>center</code> value.</>,
    },
    {
      signature: "getIntensity()",
      returns: "float",
      description: <>Returns the current <code>intensity</code> value.</>,
    },
    {
      signature: "getMode()",
      returns: "Mode",
      description: <>Returns the current <code>mode</code> value.</>,
    },
    {
      signature: "setBorder(float)",
      returns: "void",
      description: <>Sets <code>border</code>.</>,
    },
    {
      signature: "setCenter(Vector2)",
      returns: "void",
      description: <>Sets <code>center</code>.</>,
    },
    {
      signature: "setIntensity(float)",
      returns: "void",
      description: <>Sets <code>intensity</code>.</>,
    },
    {
      signature: "setMode(Mode)",
      returns: "void",
      description: <>Sets <code>mode</code>.</>,
    }
  ]}
/>
