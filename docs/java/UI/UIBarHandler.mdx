import ApiDoc from '@site/src/components/ApiDoc';

# UIBarHandler

Complete API reference for `UIBarHandler`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIBarHandler extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIBarHandler()",
      description: <>Creates a new instance of UIBarHandler.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "align",
      type: "Align",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAlign</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
