import ApiDoc from '@site/src/components/ApiDoc';

# UISharpen

Complete API reference for `UISharpen`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UISharpen extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UISharpen()",
      description: <>Creates a new instance of UISharpen.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "edgeThreshold",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getEdgeThreshold</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "intensity",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getIntensity</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "sharpenStrength",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSharpenStrength</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
