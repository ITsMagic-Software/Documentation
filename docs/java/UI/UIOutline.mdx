import ApiDoc from '@site/src/components/ApiDoc';

# UIOutline

Complete API reference for `UIOutline`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIOutline extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIOutline()",
      description: <>Creates a new instance of UIOutline.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "outlineColor",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getOutlineColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "thickness",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getThickness</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "threshold",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getThreshold</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
