import ApiDoc from '@site/src/components/ApiDoc';

# UIAspectRatio

Complete API reference for `UIAspectRatio`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIAspectRatio extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIAspectRatio()",
      description: <>Creates a new instance of UIAspectRatio.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "mode",
      type: "Mode",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMode</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "proportion",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getProportion</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
