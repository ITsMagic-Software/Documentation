import ApiDoc from '@site/src/components/ApiDoc';

# UIGaussianBlur

Complete API reference for `UIGaussianBlur`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIGaussianBlur extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIGaussianBlur()",
      description: <>Creates a new instance of UIGaussianBlur.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "blurRadius",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getBlurRadius</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "quality",
      type: "Quality",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getQuality</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
