import ApiDoc from '@site/src/components/ApiDoc';

# UIKeyEventListener

Complete API reference for `UIKeyEventListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIKeyEventListener extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIKeyEventListener()",
      description: <>Creates a new instance of UIKeyEventListener.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "keyName",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getKeyName</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
