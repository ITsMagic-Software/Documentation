import ApiDoc from '@site/src/components/ApiDoc';

# UIFadeOut

Complete API reference for `UIFadeOut`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIFadeOut extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIFadeOut()",
      description: <>Creates a new instance of UIFadeOut.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "opacity",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getOpacity</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
