import ApiDoc from '@site/src/components/ApiDoc';

# UIVerticalScrollView

Complete API reference for `UIVerticalScrollView`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIVerticalScrollView extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIVerticalScrollView()",
      description: <>Creates a new instance of UIVerticalScrollView.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "scroll",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getScroll</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
