import ApiDoc from '@site/src/components/ApiDoc';

# UIRadioButton

Complete API reference for `UIRadioButton`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIRadioButton extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIRadioButton()",
      description: <>Creates a new instance of UIRadioButton.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "checked",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isChecked</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "pressed",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isPressed</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "down",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isDown</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "up",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isUp</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "checkedTexture",
      type: "Texture",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getCheckedTexture</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "uncheckedTexture",
      type: "Texture",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getUncheckedTexture</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "isChecked()",
      returns: "boolean",
      description: <>Returns whether <code>checked</code> is true.</>,
    },
    {
      signature: "setChecked(boolean)",
      returns: "void",
      description: <>Sets <code>checked</code>.</>,
    },
    {
      signature: "isPressed()",
      returns: "boolean",
      description: <>Returns whether <code>pressed</code> is true.</>,
    },
    {
      signature: "isDown()",
      returns: "boolean",
      description: <>Returns whether <code>down</code> is true.</>,
    },
    {
      signature: "isUp()",
      returns: "boolean",
      description: <>Returns whether <code>up</code> is true.</>,
    },
    {
      signature: "getColor()",
      returns: "Color",
      description: <>Returns the current <code>color</code> value.</>,
    },
    {
      signature: "getCheckedTexture()",
      returns: "Texture",
      description: <>Returns the current <code>checkedTexture</code> value.</>,
    },
    {
      signature: "getUncheckedTexture()",
      returns: "Texture",
      description: <>Returns the current <code>uncheckedTexture</code> value.</>,
    },
    {
      signature: "setColor(Color)",
      returns: "void",
      description: <>Sets <code>color</code>.</>,
    },
    {
      signature: "setCheckedTexture(Texture)",
      returns: "void",
      description: <>Sets <code>checkedTexture</code>.</>,
    },
    {
      signature: "setUncheckedTexture(Texture)",
      returns: "void",
      description: <>Sets <code>uncheckedTexture</code>.</>,
    }
  ]}
/>
