import ApiDoc from '@site/src/components/ApiDoc';

# UIGridLayout

Complete API reference for `UIGridLayout`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIGridLayout extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIGridLayout()",
      description: <>Creates a new instance of UIGridLayout.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "autoColumns",
      type: "boolean",
      access: "set",
      description: <>Virtual attribute exposed by <code>setAutoColumns</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "columns",
      type: "int",
      access: "set",
      description: <>Virtual attribute exposed by <code>setColumns</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "spacing",
      type: "int",
      access: "set",
      description: <>Virtual attribute exposed by <code>setSpacing</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "getSpacing()",
      returns: "int",
      description: <>Returns the current <code>spacing</code> value.</>,
    },
    {
      signature: "getColumns()",
      returns: "int",
      description: <>Returns the current <code>columns</code> value.</>,
    },
    {
      signature: "setSpacing(int)",
      returns: "void",
      description: <>Sets <code>spacing</code>.</>,
    },
    {
      signature: "setColumns(int)",
      returns: "void",
      description: <>Sets <code>columns</code>.</>,
    },
    {
      signature: "isAutoColumns()",
      returns: "boolean",
      description: <>Returns whether <code>autoColumns</code> is true.</>,
    },
    {
      signature: "setAutoColumns(boolean)",
      returns: "void",
      description: <>Sets <code>autoColumns</code>.</>,
    }
  ]}
/>
