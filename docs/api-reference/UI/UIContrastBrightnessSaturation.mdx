import ApiDoc from '@site/src/components/ApiDoc';

# UIContrastBrightnessSaturation

Complete API reference for `UIContrastBrightnessSaturation`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIContrastBrightnessSaturation extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIContrastBrightnessSaturation()",
      description: <>Creates a new instance of UIContrastBrightnessSaturation.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "brightness",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getBrightness</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "contrast",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getContrast</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "saturation",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSaturation</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "getBrightness()",
      returns: "float",
      description: <>Returns the current <code>brightness</code> value.</>,
    },
    {
      signature: "getContrast()",
      returns: "float",
      description: <>Returns the current <code>contrast</code> value.</>,
    },
    {
      signature: "getSaturation()",
      returns: "float",
      description: <>Returns the current <code>saturation</code> value.</>,
    },
    {
      signature: "setBrightness(float)",
      returns: "void",
      description: <>Sets <code>brightness</code>.</>,
    },
    {
      signature: "setContrast(float)",
      returns: "void",
      description: <>Sets <code>contrast</code>.</>,
    },
    {
      signature: "setSaturation(float)",
      returns: "void",
      description: <>Sets <code>saturation</code>.</>,
    }
  ]}
/>
