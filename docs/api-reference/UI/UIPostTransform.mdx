import ApiDoc from '@site/src/components/ApiDoc';

# UIPostTransform

Complete API reference for `UIPostTransform`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIPostTransform extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIPostTransform()",
      description: <>Creates a new instance of UIPostTransform.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "fOV",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getFOV</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "pivot",
      type: "Vector2",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getPivot</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "position",
      type: "Vector2",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getPosition</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "rotation",
      type: "Vector3",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRotation</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "scale",
      type: "Vector2",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getScale</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "getPosition()",
      returns: "Vector2",
      description: <>Returns the current <code>position</code> value.</>,
    },
    {
      signature: "getPivot()",
      returns: "Vector2",
      description: <>Returns the current <code>pivot</code> value.</>,
    },
    {
      signature: "getScale()",
      returns: "Vector2",
      description: <>Returns the current <code>scale</code> value.</>,
    },
    {
      signature: "getRotation()",
      returns: "Vector3",
      description: <>Returns the current <code>rotation</code> value.</>,
    },
    {
      signature: "setPosition(Vector2)",
      returns: "void",
      description: <>Sets <code>position</code>.</>,
    },
    {
      signature: "setPivot(Vector2)",
      returns: "void",
      description: <>Sets <code>pivot</code>.</>,
    },
    {
      signature: "setScale(Vector2)",
      returns: "void",
      description: <>Sets <code>scale</code>.</>,
    },
    {
      signature: "setRotation(Vector3)",
      returns: "void",
      description: <>Sets <code>rotation</code>.</>,
    },
    {
      signature: "getFOV()",
      returns: "float",
      description: <>Returns the current <code>fOV</code> value.</>,
    },
    {
      signature: "setFOV(float)",
      returns: "void",
      description: <>Sets <code>fOV</code>.</>,
    }
  ]}
/>
