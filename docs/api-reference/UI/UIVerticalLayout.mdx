import ApiDoc from '@site/src/components/ApiDoc';

# UIVerticalLayout

Complete API reference for `UIVerticalLayout`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIVerticalLayout extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIVerticalLayout()",
      description: <>Creates a new instance of UIVerticalLayout.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "spacing",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSpacing</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "getSpacing()",
      returns: "int",
      description: <>Returns the current <code>spacing</code> value.</>,
    },
    {
      signature: "setSpacing(int)",
      returns: "void",
      description: <>Sets <code>spacing</code>.</>,
    }
  ]}
/>
