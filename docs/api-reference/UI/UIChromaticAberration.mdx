import ApiDoc from '@site/src/components/ApiDoc';

# UIChromaticAberration

Complete API reference for `UIChromaticAberration`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIChromaticAberration extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIChromaticAberration()",
      description: <>Creates a new instance of UIChromaticAberration.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "falloff",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getFalloff</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "strength",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getStrength</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "getFalloff()",
      returns: "float",
      description: <>Returns the current <code>falloff</code> value.</>,
    },
    {
      signature: "getStrength()",
      returns: "float",
      description: <>Returns the current <code>strength</code> value.</>,
    },
    {
      signature: "setFalloff(float)",
      returns: "void",
      description: <>Sets <code>falloff</code>.</>,
    },
    {
      signature: "setStrength(float)",
      returns: "void",
      description: <>Sets <code>strength</code>.</>,
    }
  ]}
/>
