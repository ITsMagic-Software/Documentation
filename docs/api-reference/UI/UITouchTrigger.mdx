import ApiDoc from '@site/src/components/ApiDoc';

# UITouchTrigger

Complete API reference for `UITouchTrigger`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UITouchTrigger extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UITouchTrigger()",
      description: <>Creates a new instance of UITouchTrigger.</>
    }
  ]}
  attributes={[]}
  virtualAttributes={[
    {
      name: "selectedTouch",
      type: "Touch",
      access: "get",
      description: <>Virtual attribute exposed by <code>getSelectedTouch</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "getSelectedTouch()",
      returns: "Touch",
      description: <>Returns the current <code>selectedTouch</code> value.</>,
    },
    {
      signature: "isDown()",
      returns: "boolean",
      description: <>Returns whether <code>down</code> is true.</>,
    },
    {
      signature: "isPressed()",
      returns: "boolean",
      description: <>Returns whether <code>pressed</code> is true.</>,
    },
    {
      signature: "isUp()",
      returns: "boolean",
      description: <>Returns whether <code>up</code> is true.</>,
    }
  ]}
/>
