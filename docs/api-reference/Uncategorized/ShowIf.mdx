import ApiDoc from '@site/src/components/ApiDoc';

# ShowIf

Complete API reference for `ShowIf`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public interface ShowIf
```

<ApiDoc
  methods={[
    {
      signature: "value()",
      returns: "String",
      description: <>Executes <code>value</code> and returns after the underlying engine call finishes.</>,
    },
    {
      signature: "isTrue()",
      returns: "boolean",
      description: <>Returns whether true is enabled.</>,
    },
    {
      signature: "enumValue()",
      returns: "String",
      description: <>Executes <code>enumValue</code> and returns after the underlying engine call finishes.</>,
    }
  ]}
/>
