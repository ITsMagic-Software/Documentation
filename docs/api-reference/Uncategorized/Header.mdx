import ApiDoc from '@site/src/components/ApiDoc';

# Header

Complete API reference for `Header`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public interface Header
```

<ApiDoc
  methods={[
    {
      signature: "value()",
      returns: "String",
      description: <>Executes <code>value</code> and returns after the underlying engine call finishes.</>,
    }
  ]}
/>
