import ApiDoc from '@site/src/components/ApiDoc';

# DPCFShadow

Complete API reference for `DPCFShadow`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class DPCFShadow extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "DPCFShadow(DPCFShadow)",
      description: <>Executes <code>DPCFShadow</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "DPCFShadow(Engine)",
      description: <>Executes <code>DPCFShadow</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "DPCFShadow()",
      description: <>Executes <code>DPCFShadow</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "DPCFShadow",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "penumbraScale",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getPenumbraScale</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "penumbraRatioScale",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getPenumbraRatioScale</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
