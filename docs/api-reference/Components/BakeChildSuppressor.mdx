import ApiDoc from '@site/src/components/ApiDoc';

# BakeChildSuppressor

Complete API reference for `BakeChildSuppressor`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Components

**Component:** yes

---

```java
public class BakeChildSuppressor extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "BakeChildSuppressor(BakeChildSuppressor)",
      description: <>Creates a new instance of BakeChildSuppressor.</>,
    },
    {
      signature: "BakeChildSuppressor(Engine)",
      description: <>Creates a new instance of BakeChildSuppressor.</>,
    },
    {
      signature: "BakeChildSuppressor()",
      description: <>Creates a new instance of BakeChildSuppressor.</>,
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "BakeChildSuppressor",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>,
    }
  ]}
/>
