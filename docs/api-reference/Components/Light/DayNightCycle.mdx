import ApiDoc from '@site/src/components/ApiDoc';

# DayNightCycle

Complete API reference for `DayNightCycle`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Light

**Component:** yes

---

```java
public class DayNightCycle extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "DayNightCycle(DayNightCycle)",
      description: <>Creates a new instance of DayNightCycle.</>,
    },
    {
      signature: "DayNightCycle(Engine)",
      description: <>Creates a new instance of DayNightCycle.</>,
    },
    {
      signature: "DayNightCycle()",
      description: <>Creates a new instance of DayNightCycle.</>,
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "DayNightCycle",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>,
    }
  ]}
  virtualAttributes={[
    {
      name: "currentTimeOfDay",
      type: "float",
      access: "get/set",
      description: <>Returns the current current time of day.</>,
    },
    {
      name: "longitude",
      type: "float",
      access: "get/set",
      description: <>Returns the current longitude.</>,
    },
    {
      name: "speed",
      type: "float",
      access: "get/set",
      description: <>Returns the current speed.</>,
    }
  ]}
/>
