import ApiDoc from '@site/src/components/ApiDoc';

# OrbitalCameraFollow

Complete API reference for `OrbitalCameraFollow`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Prototyping

**Component:** yes

---

```java
public class OrbitalCameraFollow extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "OrbitalCameraFollow(OrbitalCameraFollow)",
      description: <>Executes <code>OrbitalCameraFollow</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "OrbitalCameraFollow(Engine)",
      description: <>Executes <code>OrbitalCameraFollow</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "OrbitalCameraFollow()",
      description: <>Creates a new instance of OrbitalCameraFollow.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "OrbitalCameraFollow",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "axisName",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAxisName</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "sensibilityHorizontal",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSensibilityHorizontal</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "sensibilityVertical",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSensibilityVertical</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "invertX",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isInvertX</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "invertY",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isInvertY</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "limited",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isLimited</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "targetOffset",
      type: "Vector3",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTargetOffset</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "distance",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getDistance</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "height",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getHeight</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "positionDamping",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getPositionDamping</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "maxHeightDistance",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMaxHeightDistance</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "maxPitch",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMaxPitch</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "minPitch",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMinPitch</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "rotationDamping",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRotationDamping</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "orbitPositionDamping",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getOrbitPositionDamping</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "orbitRotationDamping",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getOrbitRotationDamping</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "target",
      type: "SpatialObject",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTarget</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
