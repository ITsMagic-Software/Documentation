# API Reference (Java, Lua)

The runtime API is organized here by category for Java and Lua scripting.

- If a Java class extends `Component`, it appears under `API Reference/Components`, with the category shown as `"<Category>"`.
- All other Java classes appear under `API Reference`, with the category shown as `"<Category>"`.
- Classes without a category annotation are grouped under `Uncategorized`.
