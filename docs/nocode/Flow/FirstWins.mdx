# First Wins

**Menu:** `Flow`

## Purpose

Waits for multiple branch arrivals in the same operation and lets only the first one continue.

## Inputs

| Name | Type | Default | Details |
| --- | --- | --- | --- |
| `In 1..N` | `BRANCH` | - | Configurable amount of branch inputs. |

## Outputs

None.

## Notes

- The number of branch inputs is configured by the node card field `Inputs`.
