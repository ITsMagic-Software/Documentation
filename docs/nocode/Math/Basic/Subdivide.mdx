# Subdivide

**Menu:** `Math/Basic`

## Purpose

Applies the `Subdivide` math operation to the connected values.

## Inputs

| Name | Type | Default | Details |
| --- | --- | --- | --- |
| `A` | `DYNAMIC` | - | - |
| `B` | `DYNAMIC` | - | - |

## Editor note

When input `B` is disconnected, the editor now exposes a numeric default field even though the slot type is `DYNAMIC`.

## Outputs

| Name | Type | Default | Details |
| --- | --- | --- | --- |
| `Out` | `DYNAMIC` | - | - |
